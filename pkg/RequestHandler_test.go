package pkg

import (
	"fmt"
	"log"
	"testing"
)

func TestTrans(t *testing.T) {
	//添加蒙古语转换
	if s, err := Trans("", "          30                                                        "); err == nil {
		fmt.Println(s)
	} else {
		log.Fatalln(err)
	}
}
